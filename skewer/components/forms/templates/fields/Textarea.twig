<div class="form__col-{{oField.getClassVal()}}">
    <div class="form__item form__item--label-{{oField.settings.labelPosition}} {{ oField.settings.classModify }}">
        {% if (oField.settings.labelPosition != 'right') %}
            <div class="form__label">{{oField.settings.title}}{% if oField.settings.required==1 %} <ins class="form__mark">*</ins>{% endif %}</div>
        {% endif %}
        <div class="form__input form__input--textarea {%  if oField.getError() %} form__item--error {% endif %}">
            <textarea data-hash="{{templateForm.formHash}}" data-name="{{oField.settings.title}}" name="{{oField.settings.slug}}" id="{{oField.settings.slug}}_{{templateForm.formHash}}"
            {% if oField.showDefaultPlaceholder() %}
                      placeholder= {% if oField.settings.required==1 %} "{{oField.settings.title~'*'}}" {% else %} "{{oField.settings.title}}" {% endif %}
            {% endif %}
            {{oField.settings.specStyle}}>{{oField.value}}</textarea>
            <div class="form__textarea_icon">
                <div class="form__icon fa fa-check" style="display: none"></div>
            </div>

        </div>
        {% if (oField.settings.labelPosition == 'right') %}
            <div class="form__label">{{oField.settings.title}}{% if oField.settings.required==1 %} <ins class="form__mark">*</ins>{% endif %}</div>
        {% endif %}
    </div>
    {% if oField.settings.description!='' %}<div class="form__info">{{oField.settings.description}}</div>{% endif %}
</div>