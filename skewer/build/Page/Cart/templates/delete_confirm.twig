<div class="js-cart-confirm-delete-goods" style="display: none">
    <div class="b-confirm-window">
        <div class="confirm-window__content">{{ Lang.get('order.msg_dell') }}</div>
        <div class="confirm-window__btnbox">
            <button class="b-btnbox" data-value="1" data-fancybox-close>{{ Lang.get('order.yes') }}</button>
            <button class="b-btnbox" data-value="0" data-fancybox-close>{{ Lang.get('order.no') }}</button>
        </div>
    </div>
</div>

<div class="js-cart-confirm-clear-goods" style="display: none">
    <div class="b-confirm-window">
        <div class="confirm-window__content">{{ Lang.get('order.msg_dell_all') }}</div>
        <div class="confirm-window__btnbox">
            <button class="b-btnbox" data-value="1" data-fancybox-close>{{ Lang.get('order.yes') }}</button>
            <button class="b-btnbox" data-value="0" data-fancybox-close>{{ Lang.get('order.no') }}</button>
        </div>
    </div>
</div>